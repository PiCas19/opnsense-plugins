define service {
    use                     generic-service
    host_name               opnsense-bridge
    service_description     OPNsense Firewall Rules
    check_command           check_opnsense_bridge_rules
    check_interval          5
    retry_interval         1
}

define service {
    use                     generic-service
    host_name               opnsense-bridge
    service_description     OPNsense System Health
    check_command           check_opnsense_bridge_health
    check_interval          5
    retry_interval         1
}