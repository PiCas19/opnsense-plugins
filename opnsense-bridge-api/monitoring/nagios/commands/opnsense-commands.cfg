define command {
    command_name    check_opnsense_bridge_rules
    command_line    $USER1$/check_opnsense_bridge.sh -H $HOSTADDRESS$ -p /nagios/firewall-rules
}

define command {
    command_name    check_opnsense_bridge_health
    command_line    $USER1$/check_opnsense_bridge.sh -H $HOSTADDRESS$ -p /nagios/system-health
}